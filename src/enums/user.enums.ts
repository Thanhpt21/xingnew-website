export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum UserTag {
  POTENTIAL = 'POTENTIAL',
  VIP = 'VIP', 
  SPAM = 'SPAM',
  NEW_CUSTOMER = 'NEW_CUSTOMER',
  NEED_CARE = 'NEED_CARE',
}