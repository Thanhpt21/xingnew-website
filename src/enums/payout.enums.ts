export enum ReceiverType {
  USER = "USER",    
  STAFF = "STAFF",
  DISTRIBUTOR = "DISTRIBUTOR"  ,    
  VENDOR = "VENDOR"
}

export enum PayoutStatus {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
}